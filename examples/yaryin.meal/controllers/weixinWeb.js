var avril = require('avril');
var xml = require("node-xml");
var wxServices = avril.require('services.weixin');

module.exports = {
    'index': function (req, res, next, view) {
        var cacheKey = req.param('cacheKey');
    }
};